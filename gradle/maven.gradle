apply plugin: "maven"

repositories {
    maven {
        url System.env.NEXUS_PUBLIC_REPOSITORY_URL
        credentials {
            username System.env.NEXUS_DEPLOYMENT_USERNAME
            password System.env.NEXUS_DEPLOYMENT_PASSWORD
        }
    }
}