apply plugin: "maven"

repositories {
    maven {
        url nexusPublicRepositoryUrl
        credentials {
            username nexusDeploymentUsername
            password nexusDeploymentPassword
        }
    }
}